# Server Configuration
PORT=3001
NODE_ENV=development

# Database Connection
# For local development: postgresql://username:password@localhost:5432/database_name
# For production (Render): This will be automatically set by Render's PostgreSQL add-on
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pathways_dev"

# Optional: JWT Secret (if implementing token-based auth)
# JWT_SECRET="your-secret-key-here"
